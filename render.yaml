services:
  - type: web
    name: custom-logo-builder
    env: java
    buildCommand: cd frontend && npm install && npm run build && mkdir -p ../backend/src/main/resources/static && cp -r build/* ../backend/src/main/resources/static/ && cd ../backend && mvn clean package -DskipTests
    startCommand: java -jar backend/target/logo-builder-0.0.1-SNAPSHOT.jar
    envVars:
      - key: GEMINI_API_KEY
        value: AIzaSyATz1nPZxGT1JtDq653bpauGDqz-OMUGKo